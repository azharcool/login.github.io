import React from 'react'

function Speaker() {
    return (
        <div>
            <div>

            Speaker
            </div>
            <div>
            A loudspeaker (or speaker driver, 
            or most frequently just speaker) is an electroacoustic transducer,[1] that is,
             a device that converts an electrical audio signal into a corresponding sound.
            </div>
        </div>
    )
}

export default Speaker
